// Package resume loads checkpoints from the phase ledger and related tables,
// routes operator retry (fresh/resume), and builds cross-cycle continuation
// bundles for RunWithRetry and Resume entry paths.
package resume
