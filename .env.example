# Copy to `.env` in the repo root.
#
# Required — Postgres connection for taskapi.
# Native dev: localhost is fine.
# Docker dev: use host.docker.internal if Postgres runs on your machine — see docs/docker.md.
DATABASE_URL=postgres://user:pass@localhost:5432/hamix?sslmode=disable

#
# Optional — after git pull that changes schema (see CONTRIBUTING.md)
# .\scripts\migrate.ps1   # Windows
# ./scripts/migrate.sh     # Unix
#
# taskapi writes to ./logs under the repo (bind-mounted).
# HAMIX_LOG_LEVEL=debug   # debug | info | warn | error (default: info; case-insensitive; warn accepts "warning")
# HAMIX_DISABLE_LOGGING=1 # no JSON file; errors only on stderr
#
# Optional — Docker workspace mount (see docs/docker.md)
# HAMIX_HOST_HOME=C:/Users/me/projects
#
# Optional — production Docker (compose.prod.yml)
# Host path bind-mounted read-only for Cursor/Claude CLI passthrough.
# HAMIX_HOST_CLI_DIR=/usr/local/bin
# Map container paths to host paths for SPA display (JSON object).
# HAMIX_PATH_MAP='{"/host-home":"/Users/me","/host-cli":"/opt/homebrew/bin"}'
